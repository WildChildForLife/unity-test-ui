extends default



block content
    - var container = (sessions.length !== 0) ? 'container-fluid' : 'container';
    div(class=container)
        h2 #{title}
        if sessions.length !== 0
            table.table.table-dark
                tr
                    each header in ['ID', 'Created At', 'Ratings']
                        th #{header}

                    each session in sessions
                        tr
                            td
                                a(href="/sessions" + session.id) #{session.id}
                            td #{session.createdAt}
                            td
                                a(href="/ratings?sessionId=" + session.id) See the given ratings for this session
        else
            p Theres no session yet, please <a href="/sessions/create">create a session</a> before you can list them.